language: python
python:
    - "3.6"
    - "2.7"
sudo: false

matrix:
  include:
    - os: osx
      language: generic
# install dependecy
before_install:
    - brew update
    - brew install gcc
install: "pip install -r requirements-travis.txt"
script:
  - pytest pyswmm
