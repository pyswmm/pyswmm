language: python
python:
    - "3.6"
    - "2.7"
sudo: false

matrix:
  include:
    - os: osx
      language: generic
    - os: linux
      language: generic
# install dependecy
before_install:
    - if [`uname` == "Darwin"]; then
        brew update;
        brew install gcc;
      fi
install: "pip install -r requirements-travis.txt"
script:
  - py.test pyswmm
